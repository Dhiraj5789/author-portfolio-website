import styled from "@emotion/styled";
import { DARK_BLUE } from "../../modules/colors";

export const Section = styled.section`
  margin: 5% 10%;
  p {
    color: ${DARK_BLUE};
    margin-top: 1rem;
  }
`;
